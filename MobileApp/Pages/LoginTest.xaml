<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp.Pages.LoginTest"
             Title="Login">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,70"
            Spacing="25">

            <Label Text="Welcome Back" FontSize="28" HorizontalOptions="Center" />
        <Entry x:Name="EmailEntry"
               Placeholder="Email"
               Keyboard="Email"
               ClearButtonVisibility="WhileEditing" />
        <Entry x:Name="PasswordEntry"
               Placeholder="Password"
               IsPassword="True"
               ClearButtonVisibility="WhileEditing" />
          

            <Button
                Text="Login" 
                SemanticProperties.Hint="Confirm to send the image to the AI"
                BackgroundColor="#007aff"
                TextColor="White"
                CornerRadius="10"
                HorizontalOptions="Fill" />

            <Label Text="Don't have an account?" HorizontalOptions="Center" />
            <Button Text="Sign Up"
                  
                    BackgroundColor="Transparent"
                    TextColor="#007aff" />
            
        
        
  
      
            
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
